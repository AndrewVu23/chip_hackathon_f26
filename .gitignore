.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.DS_Store
handoff.md

# External simulators: cloned + built per third_party/README.md
third_party/ramulator2/
third_party/attacc_simulator/
# NeuPIMs has no license: cloned locally for reading, never committed
third_party/neupims/

# Results: aggregated summaries and per-run configs are committed so every
# reported number is traceable. The per-run metric CSVs (57 MB) and the
# simulator traces (12 MB) are regenerable with `make credibility`.
results/**/*.csv
!results/**/summary.csv
results/**/*.trace
results/**/*.yaml
docs/PRESENTATION_SCRIPT.md
!results/bench_alloc/extra_workloads.csv
