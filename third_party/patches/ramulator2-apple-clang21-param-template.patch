diff --git a/src/ramulator/base/param.h b/src/ramulator/base/param.h
index cba2239..ebac6b1 100644
--- a/src/ramulator/base/param.h
+++ b/src/ramulator/base/param.h
@@ -34,7 +34,7 @@ struct ParamReader {
  private:
   T parse() const {
     try {
-      return config[name].as<T>();
+      return config[name].template as<T>();
     } catch (const std::runtime_error& e) {
       throw std::runtime_error("Failed to parse param \"" + name + "\" for \"" + impl_name + "\": " + e.what());
     }
